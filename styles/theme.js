import { createTheme } from '@mui/material/styles'

const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#0f7487',
        },
        secondary: {
            main: '#80ADB0',
        },
        background: {
            default: '#E4E9ED',
        }
    }
})

export default theme