import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
    homepage: {
        marginTop: '200px'
    },
    header: {
        fontFamily: 'Calibri, sans-serif',
        fontWeight: 'bold',
        color: 'red'
    }
})